from prompts.pro_prompt import PRO_PROMPT

# Default intentionally uses the complete Pro prompt stack.
# Keep the public variable name DEFAULT_PROMPT for backend compatibility.
DEFAULT_PROMPT = PRO_PROMPT
