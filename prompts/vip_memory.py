VIP_MEMORY = r"""
VIP MEMORY INTEGRATION
======================

Use prior analysis only as context for continuity, not as evidence about the
current market.

When a new chart is available:
- compare current structure with remembered structure;
- identify actual changes;
- discard invalidated prior assumptions;
- never preserve a previous WAIT simply because it was previously returned.

Store only durable, decision-relevant facts. Never convert guesses into memory.
"""
